-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.appellation_commune_links (
  appellation_id uuid NOT NULL,
  commune_id uuid NOT NULL,
  created_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT appellation_commune_links_pkey PRIMARY KEY (appellation_id, commune_id),
  CONSTRAINT appellation_commune_links_appellation_id_fkey FOREIGN KEY (appellation_id) REFERENCES public.appellations(id),
  CONSTRAINT appellation_commune_links_commune_id_fkey FOREIGN KEY (commune_id) REFERENCES public.communes(id)
);
CREATE TABLE public.appellation_grape_links (
  appellation_id uuid NOT NULL,
  grape_id uuid NOT NULL,
  is_primary boolean DEFAULT true,
  CONSTRAINT appellation_grape_links_pkey PRIMARY KEY (appellation_id, grape_id),
  CONSTRAINT appellation_grape_links_appellation_id_fkey FOREIGN KEY (appellation_id) REFERENCES public.appellations(id),
  CONSTRAINT appellation_grape_links_grape_id_fkey FOREIGN KEY (grape_id) REFERENCES public.grapes(id)
);
CREATE TABLE public.appellation_soil_links (
  appellation_id uuid NOT NULL,
  soil_type_id uuid NOT NULL,
  CONSTRAINT appellation_soil_links_pkey PRIMARY KEY (appellation_id, soil_type_id),
  CONSTRAINT appellation_soil_links_appellation_id_fkey FOREIGN KEY (appellation_id) REFERENCES public.appellations(id),
  CONSTRAINT appellation_soil_links_soil_type_id_fkey FOREIGN KEY (soil_type_id) REFERENCES public.soil_types(id)
);
CREATE TABLE public.appellation_subregion_links (
  appellation_id uuid NOT NULL,
  subregion_id uuid NOT NULL,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT appellation_subregion_links_pkey PRIMARY KEY (appellation_id, subregion_id),
  CONSTRAINT appellation_subregion_links_appellation_id_fkey FOREIGN KEY (appellation_id) REFERENCES public.appellations(id),
  CONSTRAINT appellation_subregion_links_subregion_id_fkey FOREIGN KEY (subregion_id) REFERENCES public.wine_subregions(id)
);
CREATE TABLE public.appellations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slug character varying NOT NULL UNIQUE,
  name_fr character varying NOT NULL,
  name_en character varying NOT NULL,
  area_hectares numeric,
  producer_count integer,
  production_volume_hl integer,
  price_range_min_eur numeric,
  price_range_max_eur numeric,
  history_fr text,
  history_en text,
  colors_grapes_fr text,
  colors_grapes_en text,
  soils_description_fr text,
  soils_description_en text,
  geojson jsonb,
  centroid_lat double precision,
  centroid_lng double precision,
  is_premium boolean DEFAULT true,
  status character varying DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  published_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  deleted_at timestamp without time zone,
  CONSTRAINT appellations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.appellations_backup2 (
  id uuid,
  slug character varying,
  name_fr character varying,
  name_en character varying,
  area_hectares numeric,
  producer_count integer,
  production_volume_hl integer,
  price_range_min_eur numeric,
  price_range_max_eur numeric,
  history_fr text,
  history_en text,
  colors_grapes_fr text,
  colors_grapes_en text,
  soils_description_fr text,
  soils_description_en text,
  geojson jsonb,
  centroid_lat double precision,
  centroid_lng double precision,
  is_premium boolean,
  status character varying,
  published_at timestamp without time zone,
  created_at timestamp without time zone,
  updated_at timestamp without time zone,
  deleted_at timestamp without time zone
);
CREATE TABLE public.communes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  geometry USER-DEFINED NOT NULL,
  code_insee text,
  CONSTRAINT communes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.dictionary_terms (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slug character varying NOT NULL UNIQUE,
  term_fr character varying NOT NULL,
  term_en character varying NOT NULL,
  definition_fr text NOT NULL,
  definition_en text NOT NULL,
  examples_fr text,
  examples_en text,
  etymology_fr text,
  etymology_en text,
  related_modules jsonb,
  is_word_of_day boolean DEFAULT false,
  is_premium boolean DEFAULT false,
  free_order integer,
  status character varying DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  published_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  deleted_at timestamp without time zone,
  CONSTRAINT dictionary_terms_pkey PRIMARY KEY (id)
);
CREATE TABLE public.favorites (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  content_type character varying NOT NULL CHECK (content_type::text = ANY (ARRAY['appellation'::character varying, 'grape'::character varying, 'soil_type'::character varying, 'vinification_type'::character varying, 'dictionary_term'::character varying]::text[])),
  content_id uuid NOT NULL,
  module character varying CHECK (module::text = ANY (ARRAY['vignoble'::character varying, 'cepages'::character varying, 'sols'::character varying, 'vinification'::character varying, 'dictionnaire'::character varying]::text[])),
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT favorites_pkey PRIMARY KEY (id),
  CONSTRAINT favorites_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.grapes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slug character varying NOT NULL UNIQUE,
  name_fr character varying NOT NULL,
  name_en character varying NOT NULL,
  type character varying CHECK (type::text = ANY (ARRAY['white'::character varying, 'red'::character varying, 'rose'::character varying]::text[])),
  origin_country character varying,
  origin_region_fr character varying,
  origin_region_en character varying,
  origin_latitude double precision,
  origin_longitude double precision,
  history_fr text,
  history_en text,
  crossings_fr text,
  crossings_en text,
  production_regions_fr text,
  production_regions_en text,
  viticultural_traits_fr text,
  viticultural_traits_en text,
  tasting_traits_fr text,
  tasting_traits_en text,
  emblematic_wines_fr text,
  emblematic_wines_en text,
  is_premium boolean DEFAULT true,
  status character varying DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  published_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  deleted_at timestamp without time zone,
  production_countries jsonb DEFAULT '[]'::jsonb,
  CONSTRAINT grapes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.invoices (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  subscription_id uuid,
  stripe_invoice_id character varying UNIQUE,
  amount_eur numeric,
  status character varying CHECK (status::text = ANY (ARRAY['paid'::character varying, 'open'::character varying, 'void'::character varying, 'uncollectible'::character varying, 'draft'::character varying]::text[])),
  invoice_pdf_url text,
  paid_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT invoices_pkey PRIMARY KEY (id),
  CONSTRAINT invoices_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT invoices_subscription_id_fkey FOREIGN KEY (subscription_id) REFERENCES public.subscriptions(id)
);
CREATE TABLE public.news_articles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slug character varying NOT NULL UNIQUE,
  title_fr character varying NOT NULL,
  title_en character varying NOT NULL,
  excerpt_fr text,
  excerpt_en text,
  content_fr text,
  content_en text,
  cover_url text,
  module_tag character varying,
  content_type character varying,
  linked_id uuid,
  is_premium_early boolean DEFAULT false,
  status character varying DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  published_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  deleted_at timestamp without time zone,
  CONSTRAINT news_articles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.notification_preferences (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  notify_news_email boolean DEFAULT false,
  notify_daily_question boolean DEFAULT true,
  notify_quiz_reminders boolean DEFAULT false,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT notification_preferences_pkey PRIMARY KEY (id),
  CONSTRAINT notification_preferences_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.oauth_accounts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  provider character varying NOT NULL,
  provider_id character varying NOT NULL,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT oauth_accounts_pkey PRIMARY KEY (id),
  CONSTRAINT oauth_accounts_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.password_reset_tokens (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  token_hash text NOT NULL UNIQUE,
  expires_at timestamp without time zone NOT NULL,
  used_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT password_reset_tokens_pkey PRIMARY KEY (id),
  CONSTRAINT password_reset_tokens_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.quiz_answers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  session_id uuid NOT NULL,
  question_id uuid NOT NULL,
  selected_option character CHECK (selected_option = ANY (ARRAY['a'::bpchar, 'b'::bpchar, 'c'::bpchar, 'd'::bpchar])),
  is_correct boolean,
  answered_at timestamp without time zone DEFAULT now(),
  CONSTRAINT quiz_answers_pkey PRIMARY KEY (id),
  CONSTRAINT quiz_answers_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.quiz_sessions(id),
  CONSTRAINT quiz_answers_question_id_fkey FOREIGN KEY (question_id) REFERENCES public.quiz_questions(id)
);
CREATE TABLE public.quiz_question_links (
  quiz_id uuid NOT NULL,
  question_id uuid NOT NULL,
  position integer NOT NULL,
  CONSTRAINT quiz_question_links_pkey PRIMARY KEY (quiz_id, question_id),
  CONSTRAINT fk_quiz FOREIGN KEY (quiz_id) REFERENCES public.quizzes(id),
  CONSTRAINT fk_question FOREIGN KEY (question_id) REFERENCES public.quiz_questions(id)
);
CREATE TABLE public.quiz_questions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  type character varying CHECK (type::text = ANY (ARRAY['daily'::character varying, 'beginner'::character varying, 'intermediate'::character varying, 'expert'::character varying, 'thematic'::character varying]::text[])),
  theme character varying,
  question_fr text NOT NULL,
  question_en text NOT NULL,
  option_a_fr character varying NOT NULL,
  option_a_en character varying NOT NULL,
  option_b_fr character varying NOT NULL,
  option_b_en character varying NOT NULL,
  option_c_fr character varying,
  option_c_en character varying,
  option_d_fr character varying,
  option_d_en character varying,
  correct_option character NOT NULL CHECK (correct_option = ANY (ARRAY['a'::bpchar, 'b'::bpchar, 'c'::bpchar, 'd'::bpchar])),
  explanation_fr text,
  explanation_en text,
  related_module character varying,
  scheduled_date date,
  is_premium boolean DEFAULT false,
  status character varying DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  difficulty character varying CHECK (difficulty::text = ANY (ARRAY['easy'::character varying, 'medium'::character varying, 'hard'::character varying]::text[])),
  CONSTRAINT quiz_questions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.quiz_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  quiz_type character varying CHECK (quiz_type::text = ANY (ARRAY['daily'::character varying, 'beginner'::character varying, 'intermediate'::character varying, 'expert'::character varying, 'thematic'::character varying]::text[])),
  theme character varying,
  score integer DEFAULT 0,
  total integer NOT NULL,
  score_pct numeric,
  time_taken_s integer,
  completed_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  quiz_id uuid,
  xp_awarded boolean NOT NULL DEFAULT false,
  CONSTRAINT quiz_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT quiz_sessions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT fk_quiz_sessions_quiz FOREIGN KEY (quiz_id) REFERENCES public.quizzes(id)
);
CREATE TABLE public.quizzes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  type character varying NOT NULL CHECK (type::text = ANY (ARRAY['beginner'::character varying, 'intermediate'::character varying, 'expert'::character varying, 'thematic'::character varying]::text[])),
  theme character varying,
  title_fr text NOT NULL,
  title_en text,
  description_fr text,
  description_en text,
  question_count integer DEFAULT 0,
  duration_sec integer,
  is_premium boolean DEFAULT false,
  status character varying DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT quizzes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.soil_types (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slug character varying NOT NULL UNIQUE,
  name_fr character varying NOT NULL,
  name_en character varying NOT NULL,
  photo_url text,
  geological_origin_fr text,
  geological_origin_en text,
  regions_fr text,
  regions_en text,
  mineral_composition_fr text,
  mineral_composition_en text,
  wine_influence_fr text,
  wine_influence_en text,
  emblematic_aop_fr text,
  emblematic_aop_en text,
  carousel_order integer,
  is_premium boolean DEFAULT true,
  status character varying DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  published_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  deleted_at timestamp without time zone,
  CONSTRAINT soil_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.spatial_ref_sys (
  srid integer NOT NULL CHECK (srid > 0 AND srid <= 998999),
  auth_name character varying,
  auth_srid integer,
  srtext character varying,
  proj4text character varying,
  CONSTRAINT spatial_ref_sys_pkey PRIMARY KEY (srid)
);
CREATE TABLE public.subscriptions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  stripe_customer_id character varying UNIQUE,
  stripe_subscription_id character varying UNIQUE,
  stripe_price_id character varying,
  plan character varying CHECK (plan::text = ANY (ARRAY['free'::character varying, 'premium'::character varying]::text[])),
  status character varying CHECK (status::text = ANY (ARRAY['active'::character varying, 'canceled'::character varying, 'past_due'::character varying, 'trialing'::character varying, 'incomplete'::character varying, 'incomplete_expired'::character varying, 'unpaid'::character varying]::text[])),
  current_period_start timestamp without time zone,
  current_period_end timestamp without time zone,
  canceled_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT subscriptions_pkey PRIMARY KEY (id),
  CONSTRAINT subscriptions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.tasting_sheets (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  eye_color character varying,
  eye_robe character varying,
  eye_intensity character varying,
  eye_tears character varying,
  eye_notes text,
  nose_first_nose text,
  nose_second_nose text,
  nose_aroma_families jsonb,
  nose_intensity character varying,
  nose_notes text,
  mouth_attack character varying,
  mouth_mid character varying,
  mouth_finish character varying,
  mouth_acidity integer CHECK (mouth_acidity >= 1 AND mouth_acidity <= 10),
  mouth_tannins integer CHECK (mouth_tannins >= 1 AND mouth_tannins <= 10),
  mouth_alcohol integer CHECK (mouth_alcohol >= 1 AND mouth_alcohol <= 10),
  mouth_sugar integer CHECK (mouth_sugar >= 1 AND mouth_sugar <= 10),
  mouth_length_caudalie integer CHECK (mouth_length_caudalie >= 1 AND mouth_length_caudalie <= 10),
  mouth_notes text,
  wine_name character varying,
  vintage integer CHECK (vintage >= 1700 AND vintage <= EXTRACT(year FROM now())::integer),
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT tasting_sheets_pkey PRIMARY KEY (id),
  CONSTRAINT tasting_sheets_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.user_daily_question_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  question_id uuid NOT NULL,
  shown_date date NOT NULL DEFAULT CURRENT_DATE,
  is_correct boolean,
  answered_at timestamp without time zone,
  CONSTRAINT user_daily_question_log_pkey PRIMARY KEY (id),
  CONSTRAINT user_daily_question_log_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT user_daily_question_log_question_id_fkey FOREIGN KEY (question_id) REFERENCES public.quiz_questions(id)
);
CREATE TABLE public.users (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  email text NOT NULL,
  password_hash text,
  first_name character varying,
  last_name character varying,
  avatar_url text,
  role character varying DEFAULT 'user'::character varying CHECK (role::text = ANY (ARRAY['user'::character varying, 'admin'::character varying]::text[])),
  plan character varying DEFAULT 'free'::character varying CHECK (plan::text = ANY (ARRAY['free'::character varying, 'premium'::character varying]::text[])),
  plan_expires_at timestamp without time zone,
  level character varying DEFAULT 'beginner'::character varying CHECK (level::text = ANY (ARRAY['beginner'::character varying, 'amateur'::character varying, 'enthusiast'::character varying, 'expert'::character varying]::text[])),
  streak_days integer DEFAULT 0,
  is_verified boolean DEFAULT false,
  locale character varying DEFAULT 'fr'::character varying CHECK (locale::text = ANY (ARRAY['fr'::character varying, 'en'::character varying]::text[])),
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  deleted_at timestamp without time zone,
  xp integer DEFAULT 0,
  CONSTRAINT users_pkey PRIMARY KEY (id)
);
CREATE TABLE public.vinification_steps (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  vinification_type_id uuid NOT NULL,
  step_order integer NOT NULL,
  icon_url text,
  title_fr character varying NOT NULL,
  title_en character varying NOT NULL,
  summary_fr text,
  summary_en text,
  detail_fr text,
  detail_en text,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT vinification_steps_pkey PRIMARY KEY (id),
  CONSTRAINT vinification_steps_vinification_type_id_fkey FOREIGN KEY (vinification_type_id) REFERENCES public.vinification_types(id)
);
CREATE TABLE public.vinification_types (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slug character varying NOT NULL UNIQUE,
  name_fr character varying NOT NULL,
  name_en character varying NOT NULL,
  illustration_url text,
  carousel_order integer,
  is_premium boolean DEFAULT true,
  status character varying DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  published_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  deleted_at timestamp without time zone,
  CONSTRAINT vinification_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wine_regions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slug character varying NOT NULL UNIQUE,
  name_fr character varying NOT NULL,
  name_en character varying NOT NULL,
  department_count integer,
  area_hectares integer,
  total_production_hl integer,
  main_grapes_fr text,
  main_grapes_en text,
  geojson jsonb,
  centroid_lat double precision,
  centroid_lng double precision,
  color_hex character varying,
  map_order integer,
  status character varying DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  published_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  deleted_at timestamp without time zone,
  CONSTRAINT wine_regions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wine_subregions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  region_id uuid NOT NULL,
  slug character varying NOT NULL UNIQUE,
  name_fr character varying NOT NULL,
  name_en character varying NOT NULL,
  area_hectares integer,
  description_fr text,
  description_en text,
  geojson jsonb,
  centroid_lat double precision,
  centroid_lng double precision,
  map_order integer,
  status character varying DEFAULT 'draft'::character varying CHECK (status::text = ANY (ARRAY['draft'::character varying, 'published'::character varying, 'archived'::character varying]::text[])),
  published_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  deleted_at timestamp without time zone,
  CONSTRAINT wine_subregions_pkey PRIMARY KEY (id),
  CONSTRAINT wine_subregions_region_id_fkey FOREIGN KEY (region_id) REFERENCES public.wine_regions(id)
);
CREATE TABLE public.wine_subregions_backup (
  id uuid,
  region_id uuid,
  slug character varying,
  name_fr character varying,
  name_en character varying,
  area_hectares integer,
  description_fr text,
  description_en text,
  geojson jsonb,
  centroid_lat double precision,
  centroid_lng double precision,
  map_order integer,
  status character varying,
  published_at timestamp without time zone,
  created_at timestamp without time zone,
  updated_at timestamp without time zone,
  deleted_at timestamp without time zone
);